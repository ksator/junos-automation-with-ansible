---
 - name: core module junos_command
   hosts: Chris-EX4300
   connection: local
   gather_facts: no

   vars_prompt:
    - name: DEVICE_PASSWORD
      prompt: Device password
      private: yes

   tasks:
   - name: test
     junos_config:
      host: "{{ inventory_hostname }}"
      username: pytraining
      password: "{{ DEVICE_PASSWORD }}"
      commands:
      - show lldp neighbor
      - show bgp neighbor
      waitfor:
      - "result[1].Peer contains Established"
      interval: 1
      retries: 10
