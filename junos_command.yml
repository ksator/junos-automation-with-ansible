---
 - name: core module junos_command
   hosts: Chris-EX4300
   connection: local
   gather_facts: no

   vars_prompt:
    - name: DEVICE_PASSWORD
      prompt: Device password
      private: yes

   tasks:
   - name: test
     junos_command:
      host: "{{ inventory_hostname }}"
      username: pytraining
      password: "{{ DEVICE_PASSWORD }}"
      #commands:
      #- show lldp neighbor
      #- show bgp neighbor 192.168.0.2
      rpcs:
      #- "get_bgp_neighbor_information"
       - "get_bgp_neighbor_information neighbor-address="{{ item }}""
      with_items:
       - 192.168.0.2
      
