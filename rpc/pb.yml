---
 - name: pass rpc to junos devices
   hosts: AMS-EX4200
   roles:
    - Juniper.junos
   connection: local
   gather_facts: no

   tasks:

   - name: get interface information
     junos_rpc:
        host={{ inventory_hostname }}
        user=pytraining
        passwd=Poclab123
        rpc=get-interface-information
        dest={{playbook_dir}}/get_interface_information.conf
