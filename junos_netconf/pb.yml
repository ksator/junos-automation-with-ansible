---
 - name: core module junos_netconf 
   hosts: AMS-EX4300
   connection: local
   gather_facts: no
   
   tasks:
   - name: configure netconf on junos devices
     junos_netconf: 
      host: "{{ inventory_hostname }}"
      username: pytraining
      password: Poclab123
      listens_on: 830
      state: present
