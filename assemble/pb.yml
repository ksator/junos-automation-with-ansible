---
# merge all the configuration fragments into one file
 - name: create configuration
   hosts: localhost
   gather_facts: no


   tasks:

    - name: Find out pwd
      shell: pwd
      register: path

    - name: Assembling configurations
      assemble: src={{path.stdout}}/assemble/fragments dest={{path.stdout}}/assemble/junos.conf
