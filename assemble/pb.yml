---
# merge all the configuration fragments into one file
 - name: create configuration
   hosts: localhost
   gather_facts: no


   tasks:

    - name: Assembling configurations
      assemble: src=/home/ksator/ansible-training-for-junos/fragments dest=junos.conf
      
